version: 0.2

phases:
  install:
    runtime-version:
      nodejs: 10
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - './front-end/**/*'
  discard-paths: no
  base-directory: build